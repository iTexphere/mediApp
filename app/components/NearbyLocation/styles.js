import {
  StyleSheet
} from 'react-native';
import Colors from '../../utils/Colors';

export default StyleSheet.create({
  nearbyWrap:{
    backgroundColor:Colors.white,
    flexDirection:'row',
    padding:10,
    paddingBottom:10,
    paddingTop:0,
  },
  nearbyText:{
    fontSize:16,
  }
})